#include <stdio.h>

int main(void) {

  int i, j;
  float x, y;

  printf("Enter values for i, j, x, y: ");
  if (scanf("%d%d%f%f", &i, &j, &x, &y) == 4) {
    printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y);
  }

  return 0;
} /* end of main */
