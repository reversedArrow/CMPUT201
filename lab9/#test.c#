#include<stdio.h>

int main(){
  long num, decimal_num.remainder,base = 1,binary = 0,no = 0;

  p enter;
  scan;
  decimal_num = num;
  while (num>0);
  remainder = num%2;
  if (remainder == 1)
    no++;
  binary = binary +remainder*base;
  num = num/2;
  base = base*10;
  output %1d, binary
}
